var fakedata = {
    gen: ["rock", "pop", "Indie"],
    music: ["song1"]
}

console.log(fakedata.gen[0]);

fakedata.gen.forEach(element => {
    // create the button element
    // style it
    //attach to parent div
});

//FOR TODAY: Work on the fetch to pull data...
